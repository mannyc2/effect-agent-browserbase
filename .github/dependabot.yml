version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      actions:
        patterns: ['*']
# npm packages use the coordinated pinned upstream catalog. Upgrade that catalog
# and its integration lockfile together, then run full acceptance; no auto-merge.
